mutation ImportCollectionFile($input: ImportCollectionFileInput!) {
  ImportCollectionFile(input: $input) {
    Status
    ImportedCount
    SkippedCount
    MatchedCount
    ModifiedCount
    UpsertedCount
    Errors {
      Index
      Reason
    }
    Message
    Detail
  }
}
