{
  "name": "whodb-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:current": "./prebuild-current.sh && tauri build --target current",
    "build:all": "./prebuild.sh && tauri build",
    "prebuild": "./prebuild.sh",
    "prebuild:current": "./prebuild-current.sh"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.11"
  }
}