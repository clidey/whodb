---
title: "Editing Records"
description: "Edit database records inline from WhoDB's data grid — update cell values, preview changes, and save with keyboard shortcuts"
---

# Editing Records

WhoDB shows edit actions only when the connected source and selected object expose update support.

## Edit From The Context Menu

<Steps>
<Step title="Open A Data Page">
Open a storage unit and click **Data**.
</Step>
<Step title="Open Row Actions">
Right-click a row, or hover the row and use the row actions button.
</Step>
<Step title="Choose Edit Row">
Select **Edit Row**. A right-side sheet opens with the current row values.
</Step>
<Step title="Update Values">
Change fields and click **Update**.
</Step>
</Steps>

## Keyboard Editing

When table shortcuts are active, focus a row and press `Enter` or `Mod + E` to open the edit sheet.

## Notes

- The edit sheet keeps original row values so the backend can identify the target row.
- Long values render in a textarea; shorter values render in inputs.
- The grid refreshes after a successful update.
- If the source cannot identify or update the row, the backend returns an error toast.
