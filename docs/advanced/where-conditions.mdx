---
title: "WHERE Conditions"
description: "Filter Storage Unit data with the current WHERE condition builder"
---

# WHERE Conditions

WHERE conditions add server-side filters to the Storage Unit data page. They are separate from the Search field, which only searches currently loaded grid rows.

![Where Conditions](/images/16-data-view-where-conditions-popover.png)

## Add A Condition

<Steps>
<Step title="Open A Data Page">
Open a table or collection from Storage Units.
</Step>
<Step title="Open Where Condition">
Click **Add** beside the **Where condition** label.
</Step>
<Step title="Choose Field">
Select a field from the field dropdown.
</Step>
<Step title="Choose Operator And Value">
Pick an operator and enter the value.
</Step>
<Step title="Add The Condition">
Click **Add Condition** or the add control in the condition UI.
</Step>
<Step title="Run The Query">
Click the page-level **Query** button to reload the grid with the condition applied.
</Step>
</Steps>

![Where Field Dropdown](/images/17-data-view-where-field-dropdown.png)

## Operators

The operator dropdown lists the database's own operator tokens, provided by the backend for the connected source. For a typical SQL database such as PostgreSQL, the set includes:

- `=`, `!=`, `<>`
- `>`, `>=`, `<`, `<=`
- `BETWEEN`, `NOT BETWEEN`
- `LIKE`, `NOT LIKE`, `ILIKE`, `NOT ILIKE`
- `IN`, `NOT IN`
- `IS NULL`, `IS NOT NULL`

The exact list varies by database.

![Equals Operator](/images/59-where-operator-equals.png)

![Not Equals Operator](/images/62-where-operator-not-equals.png)

![Greater Than Operator](/images/60-where-operator-greater-than.png)

![Less Than Operator](/images/61-where-operator-less-than.png)

![Greater Than Or Equal Operator](/images/63-where-operator-gte.png)

![Less Than Or Equal Operator](/images/64-where-operator-lte.png)

## Multiple Conditions

Add multiple conditions when you need a narrower result set. Conditions are combined by the current builder logic and applied when you click **Query**.

![Multiple Where Conditions](/images/39-data-view-multiple-conditions.png)

## Edit Or Remove Conditions

Use the condition controls in the WHERE condition UI to edit or remove an existing condition, then click **Query** again to refresh the grid.

## Search Versus WHERE

| Feature | Behavior |
|---------|----------|
| Search field | Searches/highlights values in the currently loaded rows |
| WHERE condition | Sends a server-side filter and changes the result set after **Query** |

Use WHERE conditions for real filtering. Use Search to quickly find a value in the rows already visible.

## Related Pages

<CardGroup cols={2}>
<Card title="Filtering And Searching" icon="filter" href="/data/filtering-searching">
Compare quick search and server-side filters.
</Card>
<Card title="Data Export Analysis" icon="file-export" href="/guides/tutorials/data-export-analysis">
Filter before exporting.
</Card>
</CardGroup>
