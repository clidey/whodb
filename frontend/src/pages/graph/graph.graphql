query GetGraph($type: DatabaseType!, $schema: String!) {
  Graph(type:$type, schema: $schema) {
    Unit {
      Name
      Attributes {
        Key
        Value
      }
    }
    Relations {
      Name
      Relationship
    }
  }
}