query GetColumns($ref: SourceObjectRefInput!) {
  Columns: SourceColumns(ref: $ref) {
    Name
    Type
    MetadataFidelity
    IsPrimary
    IsForeignKey
    ReferencedTable
    ReferencedColumn
    Length
    Precision
    Scale
  }
}
