query SettingsConfig {
    SettingsConfig {
        MetricsEnabled
        CloudProvidersEnabled
        AWSProviderEnabled
        AzureProviderEnabled
        GCPProviderEnabled
        DisableCredentialForm
        EnableNewUI
        MaxPageSize
    }
}
