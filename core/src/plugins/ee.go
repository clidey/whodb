package plugins

import (
	"github.com/clidey/whodb/core/src/engine"
)

// RegisterEEPlugins registers enterprise edition plugins if available
func RegisterEEPlugins(e *engine.Engine) {
	// EE plugins will be registered here when EE is enabled
	// For now, this is a no-op to keep things simple
}